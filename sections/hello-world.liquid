<section id="hello-world-section">
    <div class="hello-world-container">
        <h2>Hello World!</h2>
        <p>This is a custom section added to your Shopify theme.</p>
        {% if customer %}
            <p>Welcome back {{ customer.first_name }}</p>
        {% else %}
            <p>Welcome guest! Please log in for a better experience.</p>
        {% endif %}
    </div>
</section>
